<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weiwu.blog.mapper.BlogTagMapper">

    <insert id="saveBlogTag">
        insert into t_blog_tag (blog_id, tag_id) values
        <foreach collection="tagIds" item="tagId" index="index" separator=",">
            (#{blogId}, #{tagId})
        </foreach>
    </insert>

    <delete id="deleteTagsByBlogId">
        delete from  t_blog_tag where  blog_id = #{id}
    </delete>

</mapper>